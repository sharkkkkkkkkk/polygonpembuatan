{
    "name": "polygon-monorepo",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "postinstall": "cd client && npm install --legacy-peer-deps && cd ../server && npm install"
    },
    "engines": {
        "node": ">=18.0.0"
    },
    "workspaces": [
        "client",
        "server"
    ]
}